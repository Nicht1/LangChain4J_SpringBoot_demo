<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.11</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.llm</groupId>
    <artifactId>llm</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <!-- 聚合器：只做父 POM + 依赖管理，不包含源码 -->
    <packaging>pom</packaging>
    <name>LLM-Spring</name>
    <description>LLM-Spring 多模块工程</description>
    <url/>

    <!-- ============================================================
         子模块：
           llm-app  — 主项目（多出口：chat / easyRagChat / chatMemoryProvider / stream）
           llm-mini — 极简流式 Chat 模块（只保留一个流式输出出口 + 智能体）
         ============================================================ -->
    <modules>
        <module>llm-app</module>
        <module>llm-mini</module>
    </modules>

    <build>
        <plugins>
            <!-- 编译统一使用 JDK 17（Maven 自身可运行在低版本 JVM 上，fork 出 javac 17） -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <fork>true</fork>
                    <executable>D:\java17\bin\javac</executable>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>dev.langchain4j</groupId>
                <artifactId>langchain4j-bom</artifactId>
                <version>1.8.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
